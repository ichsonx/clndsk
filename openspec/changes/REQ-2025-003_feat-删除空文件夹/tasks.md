归属需求 ID：REQ-2025-003_删除空文件夹  
最后更新日期：2025-11-24
──────────────────────────────────────  

## 用户故事 1：作为用户，在命令行端根据提示一步步进行操作清理空文件夹
**归属场景**：场景 1  
**验收条件：**  
- [ ]  在进行测试的时候需要用户配合，用户配合输入数据。MUST不能自动操作该步骤。

### 任务列表
- [ ] **TASK-1.1** 在原有程序基础上添加功能，在询问完用户“是否递归遍历所有目录清理”后，询问用户“是否在清理文件完毕后，也清理空文件夹？”（1、是/yes。2、否/no。选项显示为蓝色字体）。
- [ ] **TASK-1.2**当用户选择 1 ，在准备删除空文件夹的同时显示空文件夹数量，并询问用户是否删除（1、是/yes。2、否/no。选项显示为蓝色字体）。用户选择否，则退出程序。用户选择是，则按照用户的输入（路径、是否遍历。不包括匹配关键字）进行删除空文件夹，删除的同时需要显示空文件夹删除进度。



> ✅ **关键细节**：  
> - 任务含**真实代码片段**（非伪代码），新人可直接参考  
> - 每个 `[x]` 表示“已完成”，团队可直观看到进展
  ```
