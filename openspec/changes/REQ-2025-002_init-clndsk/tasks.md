归属需求 ID：REQ-2025-002_init-clndsk  
最后更新日期：2025-11-10  
──────────────────────────────────────  

## 用户故事 1：作为用户，在命令行端根据提示一步步进行操作清理文件
**归属场景**：场景 1  
**验收条件：**  
- [ ]  在进行测试的时候需要用户配合，用户配合输入数据。MUST不能自动操作该步骤。

### 任务列表
- [x] **TASK-1.1** 启动软件后，询问用户1:清理当前目录下文件，2:清理制定路径文件（然后接受用户输入路径。接受绝对路径、相对路径。选项显示为蓝色字体）。让用户输入数字选择。若用户选择2，需检查输入路径是否存在，若不存在，提示用户后退出程序。
- [x] **TASK-1.2** 询问用户  1:是否递归遍历所有目录清理？（1、是/yes。2、否/no。选项显示为蓝色字体）让用户输入数字进行选择
- [x] **TASK-1.3** 在代码中硬编码一个变量`keypoint`，数组，存放字符串。里面的字符串是要删除文件的文件名所包含的关键字。
- [x] **TASK-1.4** 根据task-1.1、task-1.2的用户输入，筛选出所有只要文件名中包含`keypoint`中任意一个关键字符串的文件，并提示用户总共要删除文件的数量（数量显示为红色字体），询问是否进行删除？（1、是/yes。2、否/no。选项显示为蓝色字体）。若用户选择2，则退出程序。
- [x] **TASK-1.5** 若用户在task-1.4中选择1，则根据task-1.1、task-1.2的用户输入，筛选出所有只要文件名中包含`keypoint`中任意一个关键字符串的文件进行删除。删除过程中需要提示当前进度，以及删除的文件名。  



> ✅ **关键细节**：  
> - 任务含**真实代码片段**（非伪代码），新人可直接参考    
> - 每个 `[x]` 表示“已完成”，团队可直观看到进展
  ```
